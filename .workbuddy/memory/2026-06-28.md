# 2026-06-28

## Bug Fix: init.sql 列名 school → college
- **问题**: User.java 实体类字段 `college`，但 init.sql 建表列名为 `school`，MyBatis-Plus 驼峰转下划线映射后查 `college` 列，不存在报错。
- **修复**: 同步 init.sql 中两处 `school` → `college`（CREATE TABLE 列定义 + INSERT 列名），确保 git clone 后新环境直接可用。
